#|
Welcome! This is an implementation of some useful and interesting
algorithms in the Scheme programming language.

To use the project,
|#
(library (algo)
  (export)
  (import (data-structure data-structure)
          (sort sort)
          (graph graph))
  )
